<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="mn.btgt.safetyinst">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />

    <application
        android:name="mn.btgt.safetyinst.AppMain"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="mn.btgt.safetyinst.MainActivity"
            android:theme="@style/AppFullScreen"/>
        <activity
            android:name="mn.btgt.safetyinst.LoginImeiActivity"
            android:theme="@style/AppFullScreen" />
        <activity
            android:name="mn.btgt.safetyinst.SplashActivity"
            android:theme="@style/AppFullScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="mn.btgt.safetyinst.LoginListActivity"
            android:theme="@style/AppFullScreen" />
        <activity android:name="mn.btgt.safetyinst.AddInfoActivity" />

        <provider
            android:name=".SettingsProvider"
            android:authorities="mn.btgt.safetyinst"
            android:enabled="true"
            android:exported="true" >
        </provider>
    </application>

</manifest>